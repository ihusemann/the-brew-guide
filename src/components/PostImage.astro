---
import S3Image from "./S3Image.astro";

interface Props {
  src: string;
  alt: string;
}

const { src, alt } = Astro.props;
---

<!-- <CloudinaryImage src={src} alt={alt} width={700} aspectRatio="4:3" /> -->
<S3Image src={src} alt={alt} width={700} aspectRatio={4/3} />

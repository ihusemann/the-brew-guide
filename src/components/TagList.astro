---
import Tag from "./Tag.astro";

interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

<div class="flex space-x-4">
  {
    tags.map((tag) => {
      return <Tag label={tag} />;
    })
  }
</div>

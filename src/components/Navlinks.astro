---
import type { Link } from "./Navlink.astro";
import Navlink from "./Navlink.astro";

const links: Link[] = [
  {
    label: "Guides",
    href: "/guide",
    exactMatch: false,
  },
  {
    label: "Recipes",
    href: "/recipes",
  },
];
---

{
  links.map(({ label, href, exactMatch }) => (
    <Navlink label={label} href={href} exactMatch={exactMatch} />
  ))
}
